import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

type Props = {
    route: {params: {id:string}}
}

const UpdateParicionScreen = ({route}:Props) => {
  return (
    <View>
      <Text>
        
      </Text>
    </View>
  )
}

export default UpdateParicionScreen

const styles = StyleSheet.create({})